# Troubleshooting

Start with server health, database reachability, node-agent reconnect, catalog
fetch, WebSocket subscription, DNS, and cross-host overlay checks. Redact tokens,
credentials, hostnames, addresses, and user paths before sharing diagnostics.
