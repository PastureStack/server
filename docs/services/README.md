# Service compatibility

Service, stack, sidekick, health-check, scheduling, metadata, and catalog API
contracts remain legacy-compatible. New PastureStack names apply to repositories,
images, documentation, and user-facing product text; protocol identifiers are
changed only after a coordinated consumer migration.
