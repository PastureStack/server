# High availability

High-availability operation requires an external database, consistent server
configuration, health-checked traffic distribution, and a tested backup and
rollback procedure. The repository does not claim production readiness until a
multi-node disposable-VM POC passes.
