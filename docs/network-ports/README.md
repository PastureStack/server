# Network ports

Allow only the ports required by the selected host, agent, overlay, load-balancer,
and API topology. Derive the final rules from captured traffic in the disposable
VM POC. Do not copy permissive historical firewall examples into production.
